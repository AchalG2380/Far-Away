# ============================================================
#  ASL Retail Assistant — Master Python Requirements
#  Compatible with: Python 3.10  |  Windows / Linux
#
#  Install:  pip install -r requirements.txt
#
#  NOTE: Install order matters. Use SETUP.bat or follow
#        the order in HOW_TO_RUN.md for best results.
# ============================================================

# --- Web framework & server ---
fastapi==0.110.0
uvicorn[standard]==0.27.1
python-multipart==0.0.9
python-dotenv==1.0.1

# --- Core numerics (pin first — everything depends on these) ---
numpy==1.26.4
# Protobuf: mediapipe needs <5, TF 2.15 needs >=3.20.3 → 4.x is the safe zone
protobuf==4.25.9
# ml_dtypes: TF 2.15 pins ~=0.3.1 but jax 0.5.x needs >=0.5.0 → pin explicitly
ml_dtypes==0.5.4

# --- ML & Vision ---
mediapipe==0.10.14
opencv-python==4.10.0.84

# --- TensorFlow (CPU only — lighter, works on most laptops) ---
# Pin to 2.15.x — last version before TFLite internal API broke
tensorflow-cpu==2.15.1
# Modern TFLite runtime — needed for letter model compiled with TF 2.16+
ai_edge_litert

# --- JAX (pulled by mediapipe — pin to avoid jax 0.6.x which needs ml_dtypes 0.6+) ---
# jax 0.6.x requires ml_dtypes>=0.6.0 which is NOT available for Python 3.10
jax==0.5.3
jaxlib==0.5.3

# --- AI / LLM ---
groq==0.11.0

# --- Translation & TTS ---
deep-translator==1.11.4
gtts==2.5.1

# --- WebSocket relay ---
websockets==12.0

# --- Utility ---
scikit-learn==1.4.0
pandas==2.2.0
scipy==1.13.1
